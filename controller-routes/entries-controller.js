//Controller for Entries - Routes and SQL Queries
//Start with POST and GET, then add rest of CRUD once those are done

//POST (CREATE):  Add new entry

//GET (FindOne/FindAll):  View all entries by user.id AND trip.id

//GET:  View entry details by user.id AND trip.id

//PUT (UPDATE): Entry detail (i.e. title, text, date/time, reference table for fields)

//DELETE (DESTROY):  Entry (include reference to trip.id)