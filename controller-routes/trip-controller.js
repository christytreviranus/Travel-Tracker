//Controller for Trips - Routes and SQL Queries
//Start with POST and GET, then add rest of CRUD once those are done

//POST (CREATE):  Add new trip

//GET (FindOne/FindAll):  View all trips by user.id

//PUT (UPDATE):  Trip detail (i.e. title, start date, end date, reference table for fields)

//DELETE (DESTROY):  Trip (include all entries within trip)
